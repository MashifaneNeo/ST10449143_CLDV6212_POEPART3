@model IEnumerable<ST10449143_CLDV6212_POEPART1.Models.Order>
@{
    ViewData["Title"] = "Orders";
    var isAdmin = Context.Session.GetString("Role") == "Admin";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>
        <i class="fas fa-shopping-cart"></i>
        @(isAdmin ? "All Orders" : "My Orders")
    </h2>

    @if (isAdmin)
    {
        <div>
            <a asp-controller="Customer" asp-action="Index" class="btn btn-outline-primary me-2">
                <i class="fas fa-users"></i> Manage Customers
            </a>
            <a asp-controller="Product" asp-action="Index" class="btn btn-outline-success">
                <i class="fas fa-box"></i> Manage Products
            </a>
        </div>
    }
    else
    {
        <a asp-controller="Cart" asp-action="Index" class="btn btn-primary">
            <i class="fas fa-shopping-cart"></i> View Cart
        </a>
    }
</div>

@if (isAdmin)
{
    <div class="text-center mb-5 py-4 bg-light rounded shadow-sm">
        <h5 class="fw-semibold text-dark mb-2">
            <i class="fas fa-tasks text-warning"></i> Order Management Dashboard
        </h5>
        <p class="text-muted fst-italic mb-0">
            Monitor and manage all customer orders. Update order statuses to track order progress.
        </p>
    </div>
}
else
{
    <div class="text-center mb-5 py-4 bg-light rounded shadow-sm">
        <h5 class="fw-semibold text-dark mb-2">
            <i class="fas fa-history text-warning"></i> My Order History
        </h5>
        <p class="text-muted fst-italic mb-0">
            Track your orders and view order details. Contact support if you have any questions about your purchases.
        </p>
    </div>
}

@if (Model.Any())
{
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>Order ID</th>
                    <th>Username</th>
                    <th>Product</th>
                    <th>Order Date</th>
                    <th>Quantity</th>
                    <th>Total Price</th>
                    <th>Status</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var order in Model.OrderByDescending(o => o.OrderDate))
                {
                    <tr>
                        <td><code>@order.Id.Substring(0, 8)...</code></td>
                        <td>@order.Username</td>
                        <td>@order.ProductName</td>
                        <td>@order.OrderDate.ToString("MMM dd, yyyy")</td>
                        <td>@order.Quantity</td>
                        <td>
                            <strong class="text-success">@order.TotalPrice.ToString("C")</strong>
                        </td>
                        <td>
                            <span class="badge bg-@(GetStatusBadgeClass(order.Status)) me-2">
                                @order.Status
                            </span>

                            @if (isAdmin)
                            {
                                <div class="dropdown d-inline">
                                    <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button"
                                            data-bs-toggle="dropdown" aria-expanded="false">
                                        <i class="fas fa-cog"></i>
                                    </button>
                                    <ul class="dropdown-menu">
                                        <li>
                                            <a class="dropdown-item" href="#"
                                               onclick="updateOrderStatus('@order.Id', 'Submitted')">
                                                <i class="fas fa-clock text-primary me-2"></i>Set to Submitted
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" href="#"
                                               onclick="updateOrderStatus('@order.Id', 'Processing')">
                                                <i class="fas fa-cog text-info me-2"></i>Set to Processing
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" href="#"
                                               onclick="updateOrderStatus('@order.Id', 'Processed')">
                                                <i class="fas fa-check-double text-success me-2"></i>Set to Processed
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" href="#"
                                               onclick="updateOrderStatus('@order.Id', 'Completed')">
                                                <i class="fas fa-flag-checkered text-success me-2"></i>Set to Completed
                                            </a>
                                        </li>
                                        <li>
                                            <a class="dropdown-item" href="#"
                                               onclick="updateOrderStatus('@order.Id', 'Cancelled')">
                                                <i class="fas fa-times-circle text-danger me-2"></i>Set to Cancelled
                                            </a>
                                        </li>
                                    </ul>
                                </div>
                            }
                        </td>
                        <td>
                            <div class="btn-group" role="group">
                                <a asp-action="Details" asp-route-id="@order.Id" class="btn btn-sm btn-outline-info" title="View Details">
                                    <i class="fas fa-eye"></i>
                                </a>

                                @if (isAdmin)
                                {
                                    <a asp-action="Edit" asp-route-id="@order.Id" class="btn btn-sm btn-outline-primary" title="Edit Order">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <button type="button" class="btn btn-sm btn-outline-danger"
                                            onclick="confirmDelete('@order.Id', '@order.Id')" title="Delete Order">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Order Statistics for Admin -->
    @if (isAdmin)
    {
        <div class="row mt-4">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <h6 class="card-title mb-0">
                            <i class="fas fa-chart-bar me-2"></i>Order Statistics
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="row text-center">
                            <div class="col-md-2">
                                <div class="border rounded p-3">
                                    <h5 class="text-primary">@Model.Count()</h5>
                                    <small class="text-muted">Total Orders</small>
                                </div>
                            </div>
                            <div class="col-md-2">
                                <div class="border rounded p-3">
                                    <h5 class="text-info">@Model.Count(o => o.Status == "Submitted")</h5>
                                    <small class="text-muted">Submitted</small>
                                </div>
                            </div>
                            <div class="col-md-2">
                                <div class="border rounded p-3">
                                    <h5 class="text-warning">@Model.Count(o => o.Status == "Processing")</h5>
                                    <small class="text-muted">Processing</small>
                                </div>
                            </div>
                            <div class="col-md-2">
                                <div class="border rounded p-3">
                                    <h5 class="text-success">@Model.Count(o => o.Status == "Processed")</h5>
                                    <small class="text-muted">Processed</small>
                                </div>
                            </div>
                            <div class="col-md-2">
                                <div class="border rounded p-3">
                                    <h5 class="text-success">@Model.Count(o => o.Status == "Completed")</h5>
                                    <small class="text-muted">Completed</small>
                                </div>
                            </div>
                            <div class="col-md-2">
                                <div class="border rounded p-3">
                                    <h5 class="text-danger">@Model.Count(o => o.Status == "Cancelled")</h5>
                                    <small class="text-muted">Cancelled</small>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    }
}
else
{
    <div class="alert alert-info text-center py-5">
        <div class="mb-4">
            <i class="fas fa-shopping-cart fa-4x text-muted"></i>
        </div>
        <h3 class="text-muted">No orders found</h3>
        <p class="text-muted mb-4">
            @(isAdmin ? "There are no orders in the system yet." : "You haven't placed any orders yet.")
        </p>
        @if (!isAdmin)
        {
            <a asp-controller="Product" asp-action="Index" class="btn btn-primary btn-lg">
                <i class="fas fa-shopping-bag me-2"></i>Start Shopping
            </a>
        }
    </div>
}

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">
                    <i class="fas fa-exclamation-triangle me-2"></i>Confirm Delete
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete order <strong id="orderId"></strong>?
                <div class="alert alert-warning mt-2">
                    <i class="fas fa-info-circle me-2"></i>
                    This action cannot be undone.
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form id="deleteForm" method="post" style="display: inline;">
                    <button type="submit" class="btn btn-danger">Delete Order</button>
                </form>
            </div>
        </div>
    </div>
</div>

@{
    string GetStatusBadgeClass(string status) => status?.ToLower() switch
    {
        "submitted" => "primary",
        "processing" => "info",
        "processed" => "success",
        "completed" => "success",
        "cancelled" => "danger",
        _ => "secondary"
    };
}

@section Scripts {
    <script>
        function confirmDelete(orderId, orderDisplayId) {
            document.getElementById('orderId').textContent = orderDisplayId.substring(0, 8) + '...';
            document.getElementById('deleteForm').action = '/Order/Delete/' + orderId;
            new bootstrap.Modal(document.getElementById('deleteModal')).show();
        }

        function updateOrderStatus(orderId, newStatus) {
            if (confirm(`Are you sure you want to change order status to "${newStatus}"?`)) {
                // Show loading state
                const event = new CustomEvent('showLoading', { detail: 'Updating order status...' });
                document.dispatchEvent(event);

                fetch('/Order/UpdateStatusAjax', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
                    },
                    body: JSON.stringify({
                        id: orderId,
                        newStatus: newStatus
                    })
                })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        showNotification('Order status updated successfully!', 'success');
                        setTimeout(() => location.reload(), 1000);
                    } else {
                        showNotification('Error: ' + data.message, 'error');
                    }
                })
                .catch(error => {
                    showNotification('An error occurred while updating the status', 'error');
                });
            }
        }

        // Initialize page
        document.addEventListener('DOMContentLoaded', function() {
            // Add any order-specific initialization here
        });
    </script>
}